<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.NextGIS.AppCompat.Light">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="ThemeOverlay.NextGIS.Toolbar">@style/Theme.Woody.ActionBarTheme</item>
    </style>

    <style name="Theme.Woody.ActionBarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/textColorPrimary.Light</item>
        <item name="titleTextColor">@color/textColorPrimary.Light</item>
        <item name="subtitleTextColor">@color/textColorSecondary.Light</item>
    </style>

</resources>
